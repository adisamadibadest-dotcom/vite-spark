// Stub: TanStack Start server middleware is not used in this Vite/Vercel setup.
// Auth is handled by the Supabase client on the frontend and
// the Authorization header is validated server-side per-request.
export const requireSupabaseAuth = null;
